var result = (async () => {
  const s = false;
  var i = class {
    constructor(r, e, n) {
      this.debug = r;
      this.prefix = e;
      this.isTestMode = n;
    }
    log(r, e) {
      if (this.debug) {
        console.log(`${this.prefix} ${r}`, e != null ? e : "");
      }
    }
    error(r, e) {
      console.error(`${this.prefix} ${r}`, e != null ? e : "");
    }
    warn(r, e) {
      console.warn(`${this.prefix} ${r}`, e != null ? e : "");
    }
    /**
     * Prints a test mode header with separator lines
     * Only outputs if testMode is enabled
     */
    testHeader(r, e) {
      if (!this.isTestMode) {
        return;
      }
      const n = "=".repeat(80);
      console.debug(n);
      console.debug(r);
      console.debug(n);
      if (e !== void 0) {
        console.debug(e);
        console.debug(n);
      }
    }
    /**
     * Prints test mode result output with separator lines
     * Only outputs if testMode is enabled
     */
    testResult(r) {
      if (!this.isTestMode) {
        return;
      }
      const e = "=".repeat(80);
      console.debug(e);
      console.debug("RESULT:");
      console.debug(e);
      if (typeof r === "string") {
        console.debug(r);
      } else {
        console.debug(JSON.stringify(r, null, 2));
      }
      console.debug(e);
    }
    /**
     * Prints additional test mode info
     * Only outputs if testMode is enabled
     */
    testInfo(r, e) {
      if (!this.isTestMode) {
        return;
      }
      if (e !== void 0) {
        console.debug(r, e);
      } else {
        console.debug(r);
      }
    }
  };
  function u(r, e, n) {
    const t = n ? `[${e} Test]` : `[AEP ${e}]`;
    return new i(r, t, n);
  }
  function l(r) {
    var e;
    const n = `; ${document.cookie}`;
    const t = n.split(`; ${r}=`);
    if (t.length === 2) {
      const o = (e = t.pop()) == null ? void 0 : e.split(";").shift();
      return o != null ? o : null;
    }
    return null;
  }
  function c(r) {
    if (!r) {
      return null;
    }
    try {
      return JSON.parse(decodeURIComponent(r));
    } catch (e) {
      return null;
    }
  }
  var a = "partner_data";
  function g(r, e) {
    const n = l(r);
    if (!n) {
      e.log("No partner data in cookies");
      return null;
    }
    const t = c(n);
    if (!t) {
      e.error("Error parsing partner data from cookie");
      return null;
    }
    e.log("Found partner data", t);
    return t;
  }
  async function f(r = false, e = a) {
    const n = {
      debug: r,
      cookieKey: e
    };
    const t = u(n.debug, "Partner Data", r);
    try {
      t.testHeader(
        "PARTNER DATA EXTRACTOR - TEST MODE",
        `Cookie Key: ${n.cookieKey}`
      );
      const o = g(n.cookieKey, t);
      t.testResult(o);
      if (!r) {
        t.log("Returning partner data", o);
      }
      return o;
    } catch (o) {
      t.error("Unexpected error fetching partner data:", o);
      return null;
    }
  }
  return await f(s);
})();
return result;
