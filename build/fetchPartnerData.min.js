var result = (async () => {
  const i = false;
  var u = class {
    constructor(r, e, t) {
      this.debug = r;
      this.prefix = e;
      this.isTestMode = t;
    }
    log(r, e) {
      if (this.debug) {
        console.log(`${this.prefix} ${r}`, e != null ? e : "");
      }
    }
    error(r, e) {
      console.error(`${this.prefix} ${r}`, e != null ? e : "");
    }
    warn(r, e) {
      console.warn(`${this.prefix} ${r}`, e != null ? e : "");
    }
    /**
     * Prints a test mode header with separator lines
     * Only outputs if testMode is enabled
     */
    testHeader(r, e) {
      if (!this.isTestMode) {
        return;
      }
      const t = "=".repeat(80);
      console.debug(t);
      console.debug(r);
      console.debug(t);
      if (e !== void 0) {
        console.debug(e);
        console.debug(t);
      }
    }
    /**
     * Prints test mode result output with separator lines
     * Only outputs if testMode is enabled
     */
    testResult(r) {
      if (!this.isTestMode) {
        return;
      }
      const e = "=".repeat(80);
      console.debug(e);
      console.debug("RESULT:");
      console.debug(e);
      if (typeof r === "string") {
        console.debug(r);
      } else {
        console.debug(JSON.stringify(r, null, 2));
      }
      console.debug(e);
    }
    /**
     * Prints additional test mode info
     * Only outputs if testMode is enabled
     */
    testInfo(r, e) {
      if (!this.isTestMode) {
        return;
      }
      if (e !== void 0) {
        console.debug(r, e);
      } else {
        console.debug(r);
      }
    }
  };
  function l(r, e, t) {
    const n = t ? `[${e} Test]` : `[AEP ${e}]`;
    return new u(r, n, t);
  }
  function c(r) {
    var e;
    const t = `; ${document.cookie}`;
    const n = t.split(`; ${r}=`);
    if (n.length === 2) {
      const o = (e = n.pop()) == null ? void 0 : e.split(";").shift();
      return o != null ? o : null;
    }
    return null;
  }
  function a(r) {
    if (!r) {
      return null;
    }
    try {
      return JSON.parse(decodeURIComponent(r));
    } catch (e) {
      return null;
    }
  }
  var g = "partner_data";
  function f(r, e) {
    const t = c(r);
    if (!t) {
      e.log("No partner data in cookies");
      return null;
    }
    const n = a(t);
    if (!n) {
      e.error("Error parsing partner data from cookie");
      return null;
    }
    e.log("Found partner data", n);
    return n;
  }
  async function d(r = false, e = g) {
    const t = {
      debug: r,
      cookieKey: e
    };
    const n = l(t.debug, "Partner Data", r);
    try {
      n.testHeader(
        "PARTNER DATA EXTRACTOR - TEST MODE",
        `Cookie Key: ${t.cookieKey}`
      );
      const o = f(t.cookieKey, n);
      const s = {
        partnerData: o
      };
      n.testResult(s);
      if (!r) {
        n.log("Returning partner data", s);
      }
      return s;
    } catch (o) {
      n.error("Unexpected error fetching partner data:", o);
      return null;
    }
  }
  return await d(i);
})();
return result;
